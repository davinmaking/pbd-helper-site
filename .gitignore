.DS_Store
# Local dev notes — not deployed to the public Pages site
CLAUDE.md
# Marketing drafts + assets — local only, not deployed
marketing/
# Session handoff notes — local only, must never deploy
HANDOFF.md
